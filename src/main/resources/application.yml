spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fuelspot_db?serverTimezone=Asia/Seoul
    username: root
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true